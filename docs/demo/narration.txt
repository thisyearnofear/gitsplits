Open source underwrites the global economy. Almost nobody pays for it. GitSplits turns enterprise OSS funding into a UiPath Maestro case — agents handling intake, repo analysis, and payout autonomously, with humans approving only the high-impact decisions. Maestro at the center, agents on the perimeter, every case auditable.

A sponsor picks a dependency, sets the amount, and submits. One form opens a case. The Approval Routing Agent predicts the autonomy tier live — from amount and risk flags.

Intake. Repo analysis runs LangChain reasoning over EigenAI-attested quality scores. Verification check. This twenty-five-hundred-dollar request lands at tier T1 — finance approves once. Split written on chain. Payout via Ping Pay. TEE attestation closes the audit trail. End to end in under five seconds.

Operators see every case across the tenant. Open, completed, total disbursed. The autonomy distribution shows how much work agents handled without paging a human. This is what enterprise OSS funding looks like — orchestrated on UiPath.
